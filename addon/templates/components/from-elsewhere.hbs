{{#if initialized}}
  {{#if hasBlock}}
    {{yield (get (get service.actives name) 'component') }}
  {{else}}
    {{#with (get (get service.actives name) 'component') as |c|}}
      {{component c}}
    {{/with}}
  {{/if}}
{{/if}}
